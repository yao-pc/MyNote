# config-配置文件
## config_auto：判定soc_info.json文件的路径
	`json.dumps()` 是 Python 标准库 `json` 模块提供的函数，用于将 Python 对象（如 `dict`、`list`）序列化为 JSON 格式的字符串。
	indent=4这个参数使生成的 JSON 字符串具有缩进，每一层缩进 4 个空格。
## config_case：包信息，逆变器信息，平台信息
## config_inv：设备类型，告警信息，
## constant：系统中常量参数
--- 
# 驱动模块 (drive)
## inst_drive：IP及端口连接
--- 
# 公用方法(func)
--- 
# 日志模块 (log)

# 协议通信模块 (prot_layer)
TCPFrame.py该文件是TCP协议帧的组包与解析工具类,主要功能:
帧头管理:维护事务序号(0-65535循环)和协议标识(0x5347),自动生成唯一帧头
接收帧解析:从TCP报文中提取源/目标地址层级、地址信
# UI资源模块 (ui_resource)

UI层，具体窗口及组件